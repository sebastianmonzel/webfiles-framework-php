language: php
 
php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6

cache:
  directories:
    - $HOME/vendor

before_script:
  - composer self-update
  - composer install --dev --prefer-source --no-interaction

script:
  - phpunit tests
  

